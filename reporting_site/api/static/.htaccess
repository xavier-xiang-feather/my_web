RewriteEngine On
RewriteBase /api/static/

# 真实存在的文件/目录就直接访问（例如你以后放文档）
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# 其他全部交给 index.php
RewriteRule ^ index.php [QSA,L]